# 플러그인에 동봉되는 배포용 공식 API 키.
# - 값이 있으면 기본 기능(배경지도 등)은 이 키로 동작한다 (설치 즉시 사용 가능).
# - 사용자 개인 키는 옵션 창에서 입력받아 QSettings에 저장된다
#   (ConfigManager.user_api_key — 엑셀 지오코딩 등은 사용자 키만 허용).
# - 이 값은 로그·오류 메시지에 원문으로 출력하면 안 된다 (ApiClient._mask_key 등으로 마스킹).
API_KEY = "E5B1657B-9B6F-3A4B-91EF-98512BE931A1"
